import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ["192.168.1.171", "fitpro-leo.loca.lt", "everywhere-instructions-created-officials.trycloudflare.com"],
};

export default nextConfig;
